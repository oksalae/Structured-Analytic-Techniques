/**
 * At repo root there is no standalone server — use start-all.js to start the hub and all tools.
 * This file runs that script so "node server.js" works from the repo root.
 */
require('./start-all.js');
